function editUserData () {
    alert("edit user data");
}


async function login () {
    /*
    const form = document.getElementById("loginForm");
    const success = await fetch (form.action, {method:'post', body: new FormData(form)});
    if (success)
        window.location.reload();*/
}